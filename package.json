{
  "name": "chrome-viewport-capture",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check background.js && node --check capture-core.js && node --check popup.js",
    "package:release": "bash scripts/package-release.sh",
    "test": "node --test tests/*.test.mjs"
  }
}
